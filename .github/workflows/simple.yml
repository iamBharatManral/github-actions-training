name: Shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echjjo 'Hello from a github action'
      - name: multiline script
        run: |
          node -v
          npm -v
    
